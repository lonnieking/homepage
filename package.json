{
  "name": "homepage",
  "version": "1.0.0",
  "description": "The personal homepage of Lonnie King.",
  "repository": "www.github.com/lonnieking/homepage",
  "main": "index.js",
  "scripts": {
    "create": "AWS_PROFILE=personal AWS_REGION=ap-southeast-2 arc-create",
    "deploy": "AWS_PROFILE=personal AWS_REGION=ap-southeast-2 arc-deploy",
    "start": "AWS_PROFILE=personal AWS_REGION=ap-southeast-2 NODE_ENV=testing nodemon --watch src -e js,css --exec arc-sandbox",
    "dns": "AWS_PROFILE=personal AWS_REGION=ap-southeast-2 arc-dns"
  },
  "author": "Lonnie King",
  "license": "ISC",
  "dependencies": {
    "@architect/workflows": "^2.2.4"
  },
  "devDependencies": {
    "tap-spec": "^4.1.1",
    "tape": "^4.8.0",
    "nodemon": "^1.14.3"
  }
}
